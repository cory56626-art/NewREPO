# TDS Wiki Bridge

This branch is isolated from the main project. Updating `request.txt` triggers GitHub Actions to fetch the matching Tower Defense Simulator Fandom page and save its raw wikitext to `result.txt`.
